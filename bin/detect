#!/usr/bin/env bash
# bin/detect <build-dir>
#!/usr/bin/env bash



if [ -f $1/Aptfile ]; then
  echo "Apt" && exit 0
else
  echo "no" && exit 1
fi
if [ -f $1/.apt/usr/bin/Xvfb ]; then
    echo "Xvfb-setup"
    exit 0
fi

exit 1